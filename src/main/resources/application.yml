server:
  port: 8080

spring:
  application:
    name: fengshen-game
  datasource:
    url: jdbc:mysql://localhost:3306/fengshen?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: fengshen-game-secret-key-2024
  expiration: 86400000
